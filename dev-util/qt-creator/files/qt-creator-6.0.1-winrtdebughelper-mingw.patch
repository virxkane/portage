diff -urBb qt-creator-opensource-src-6.0.1-orig/src/tools/winrtdebughelper/winrtdebughelper.cpp qt-creator-opensource-src-6.0.1/src/tools/winrtdebughelper/winrtdebughelper.cpp
--- qt-creator-opensource-src-6.0.1-orig/src/tools/winrtdebughelper/winrtdebughelper.cpp	2021-12-15 13:37:14.000000000 +0400
+++ qt-creator-opensource-src-6.0.1/src/tools/winrtdebughelper/winrtdebughelper.cpp	2022-01-01 03:14:51.019168000 +0400
@@ -66,7 +66,13 @@
 
     const size_t msgBufferSize = 15;
     char pidMessageBuffer[msgBufferSize];
+#if defined(_MSC_VER) || (defined(__MINGW64_VERSION_MAJOR) && MINGW_HAS_SECURE_API==1)
     int length = sprintf_s(pidMessageBuffer, msgBufferSize, "PID:%d", pid);
+#else
+    snprintf(pidMessageBuffer, msgBufferSize, "PID:%d", pid);
+    pidMessageBuffer[msgBufferSize - 1] = 0;
+    int length = strlen(pidMessageBuffer);
+#endif
     if (length >= 0)
         WriteFile(pipe, pidMessageBuffer, length, NULL, NULL);
 
